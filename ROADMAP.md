# Roadmap

This is a direction of travel, not a commitment to publish everything listed.

## Now

Two gaps, both named by the tests themselves rather than guessed at.

- **Nobody outside this project has scored anything here.** All three starter reviews for [You Have Been Given AI at Work](guides/you-have-been-given-ai-at-work.md) end on the same next step: somebody other than the author scoring the same outputs against the same rubric. Repeating runs, which those three reviews now do, rules out run-to-run noise and nothing else. A scoring bias held consistently by one scorer looks exactly like a real effect.
- **No guide here has a logged real use of its own.** The one record in [evidence](evidence/real-use-reusable-handover-workflow.md) says so on its own first line: it is real use, but not of the current guides.

## Next

- Log one genuine low-risk use of any guide when an appropriate task naturally arises.
- Record organic outside feedback if someone tries any guide.
- Re-test the internal update starter on notes with a different shape of conflict. Its two-column version failed, the rewrite fixed that failure, and an ordinary prompt still matched it, so what the starter is actually worth is still open.

## Later

These are candidate subjects, not committed guides.

- What should stay human-controlled
